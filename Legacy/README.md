# Legacy Language and Theme Snapshots

This folder stores older public snapshots that are still useful for comparison:

- `VL_VERSION_2.4.md`
- `Theme-Default_3.0.txt`

They were moved out of the repo root so the main entry points stay aligned with the latest public baseline:

- VL 4.1
- Theme 7.0
- refreshed example packs with `appCaseJsonMap`
